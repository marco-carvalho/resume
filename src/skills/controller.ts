export default function ($scope) {
    $scope.backEnd = [
        "C# (Entity, WCF, WebAPI)",
        "Node.js (npm)",
        "Python (Django)",
        "PHP (Wordpress)",
        "R"
    ];

    $scope.frontEnd = [
        "Javascript (AngularJS, Gulp)",
        "HTML (Pug)",
        "CSS (Bootstrap, Sass, Stylus)"
    ];

    $scope.infra = [
        "SQL (MySQL, SQL Server)",
        "Linux",
        "Git"
    ];

    $scope.design = [
        "Photoshop",
        "Illustrator",
        "Lightroom",
        "InDesign"
    ];
}
